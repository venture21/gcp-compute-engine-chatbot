Tool: built-in image_gen.imagegen (polish original design)
Reference: nginx-roles.png
Final image: nginx-roles-v3.png

Polish and redraw the ATTACHED ORIGINAL Nginx roles infographic. The user prefers THIS original image over a later minimalist version. Preserve its recognizable design, visual richness, colored browser/server illustrations, six numbered cards in a 3-COLUMN BY 2-ROW GRID, and the bottom chatbot network diagram. Make this same image cleaner, more consistent, and more readable. Do NOT convert it to a 2-column list, monoline symbol-only design, or sparse typographic poster.

Use a high resolution landscape canvas, approximately 3:2 (1536x1024 or higher). Clean pure white background. Retain pale blue rounded cards, navy headings, rich teal Nginx server icons, dark slate backend server icons, bright blue request arrows, teal response accents, and small circular blue number badges. These colored filled illustrations are essential to the preferred original. Remove bevels, excessive gloss, shadows, texture, and any dark header gradient. Make the icons clean flat filled illustrations with consistent outlines. Use equal card heights, uniform padding, a clear aligned header/icon/description structure in every card, matching icon scale, consistent arrow strokes, and generous gutters. Ensure no words or arrowheads touch a card edge or overlap. Clear Korean text with consistent type sizes; retain all meaning and the full explanations.

HEADER:
Title centered "Nginx의 역할과 하는 일", reduce oversized weight slightly and leave ample surrounding white space.
Subtitle centered "설정에 따라 필요한 기능을 선택해 사용합니다".

CARDS in the same positions as the reference:

01 TOP LEFT "웹 서버":
Browser on left, teal Nginx server in center, HTML/CSS/image file symbols on right. Files flow through Nginx to the browser. Small labels "브라우저", "Nginx", "HTML", "CSS", "이미지".
Bottom two aligned lines:
"HTML · CSS · 이미지 등"
"정적 파일을 직접 제공합니다"

02 TOP CENTER "역방향 프록시":
Browser → teal Nginx → dark slate app server. Show the returning response through Nginx back to the browser with neat separate thin teal arrows below the request path. Node labels "브라우저", "Nginx", "내부 앱".
Bottom lines:
"사용자의 요청을 내부 앱에 전달하고"
"앱의 응답을 돌려줍니다"

03 TOP RIGHT "TLS 처리":
Browser — a lock on the encrypted link — teal Nginx with a small server certificate icon. Labels "브라우저", "HTTPS", "Nginx", "서버 인증서" only. No user-login icon. Nginx presents its certificate; do not claim Nginx verifies its own identity.
Bottom lines:
"인증서를 제시하고"
"HTTPS 암호화 통신을 처리합니다"

04 BOTTOM LEFT "리다이렉트":
Browser and Nginx with one clear curved response arrow labeled "새 주소 안내"; show the readable label "HTTP → HTTPS" above. This is Nginx telling the browser to reconnect, not encrypting a previously sent HTTP request.
Bottom lines:
"브라우저에 새 주소를 알려"
"다시 접속하도록 안내합니다"

05 BOTTOM CENTER "로드 밸런서":
Browser → teal Nginx → three smaller identical backend server icons arranged vertically. Uniform branching arrows. Readable short labels "앱 1", "앱 2", "앱 3" instead of tiny verbose labels.
Bottom lines:
"여러 애플리케이션 서버로"
"요청을 나누어 전달합니다"

06 BOTTOM RIGHT "캐시":
Browser on left, teal Nginx on right with a small cache symbol clearly attached to Nginx. Arrow from Nginx toward browser labeled "빠른 응답". Make clear Nginx uses its stored response cache, not a standalone database directly serving the browser.
Bottom lines:
"재사용 가능한 응답을 저장하고"
"조건이 맞으면 빠르게 제공합니다"

BOTTOM FULL-WIDTH PALE TEAL BAND:
Retain original visual style with a browser icon, teal Nginx server, and dark FastAPI server, all evenly aligned.
Heading "현재 챗봇: 역방향 프록시 + TLS 처리 + 리다이렉트"
Flow browser → Nginx → FastAPI, arrow labels "HTTPS" then "내부 HTTP". Node labels "브라우저", "Nginx", "FastAPI".
Give footer content ample bottom padding and no clipping.

Accuracy: all six are available Nginx capabilities; only the three listed in the bottom band are the focus of this chatbot setup. Do not claim caching/load balancing automatically enabled. No extra paragraphs, no external logos, no watermark. This should feel like a carefully cleaned-up, professionally redrawn version of the ATTACHED original, with its explanatory diagrams intact.
